apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: resourceclaims.resource.k8s.io
  annotations:
    # Required to create a CRD in a *.k8s.io group without official Kubernetes API approval.
    api-approved.kubernetes.io: "unapproved, shim CRD for spyre-scheduler informer compatibility on clusters without resource.k8s.io/v1"
spec:
  group: resource.k8s.io
  scope: Namespaced
  names:
    plural: resourceclaims
    singular: resourceclaim
    kind: ResourceClaim
    listKind: ResourceClaimList
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: deviceclasses.resource.k8s.io
  annotations:
    api-approved.kubernetes.io: "unapproved, shim CRD for spyre-scheduler informer compatibility on clusters without resource.k8s.io/v1"
spec:
  group: resource.k8s.io
  scope: Cluster
  names:
    plural: deviceclasses
    singular: deviceclass
    kind: DeviceClass
    listKind: DeviceClassList
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
